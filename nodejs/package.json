{
  "name": "shoutrrr-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "for d in packages/*/; do echo \"== $d ==\"; bun test \"$d\" || exit 1; done",
    "typecheck": "for d in packages/*/; do echo \"== $d ==\"; bun x tsc -p \"$d/tsconfig.json\" || exit 1; done"
  }
}
